#!/bin/bash

S_LIB_FOLDER=${S_LIB_FOLDER:-/usr/lib/ws-session}
source $S_LIB_FOLDER/lib/source.sh
s_source_lib wm

if [[ -n $S_WM ]] ; then 
  s_source app/dwb.sh
  s_dwb_start "$@"
  disown -a
else
  /usr/bin/dwb $@
fi

# vim: ft=sh ts=2 et sw=2:
